@model ProjectMongoDB.Models.LaporanPenjualan

@{
    ViewData["Title"] = "Hapus Laporan";
}

<style>
    .delete-container { max-width: 760px; margin: 50px auto; padding: 0 20px; }
    .card-custom { background:#fff; border-radius:12px; box-shadow:0 2px 20px rgba(0,0,0,.08); border:1px solid #e3e6f0; overflow:hidden; }
    .card-header-custom { background: linear-gradient(135deg,#dc3545 0%, #c82333 100%); color:#fff; padding:24px 28px; text-align:center; }
    .card-header-custom h2 { margin:0 0 8px 0; font-size:24px; font-weight:600; }
    .card-body-custom { padding: 30px; }
    .detail-row { display:flex; padding:14px 0; border-bottom:1px solid #f7fafc; }
    .detail-row:last-child { border-bottom:none; }
    .detail-label { flex:0 0 200px; font-weight:600; color:#2d3748; font-size:14px; text-transform:uppercase; letter-spacing:.5px; }
    .detail-value { flex:1; color:#4a5568; font-size:15px; }
    .button-group { display:flex; gap:12px; justify-content:flex-end; margin-top: 20px; padding-top: 18px; border-top:2px solid #f7fafc; }
    .btn-custom { padding:12px 26px; border:none; border-radius:8px; font-size:15px; font-weight:600; cursor:pointer; transition:all .2s ease; text-decoration:none; display:inline-block; }
    .btn-secondary-custom { background:#f7fafc; color:#4a5568; border:2px solid #e2e8f0; }
    .btn-secondary-custom:hover { background:#edf2f7; }
    .btn-danger-custom { background: linear-gradient(135deg,#dc3545 0%, #c82333 100%); color:#fff; }
</style>

<div class="delete-container">
    <div class="card-custom">
        <div class="card-header-custom">
            <h2>🗑️ Hapus Laporan Penjualan</h2>
            <p>Apakah Anda yakin ingin menghapus data ini?</p>
        </div>
        <div class="card-body-custom">
            <div class="detail-row">
                <div class="detail-label">Tanggal</div>
                <div class="detail-value">@Model.Tanggal.ToString("yyyy-MM-dd")</div>
            </div>
            <div class="detail-row">
                <div class="detail-label">Nama Produk</div>
                <div class="detail-value">@Model.NamaProduk</div>
            </div>
            <div class="detail-row">
                <div class="detail-label">Jumlah Terjual</div>
                <div class="detail-value">@Model.JumlahTerjual</div>
            </div>
            <div class="detail-row">
                <div class="detail-label">Harga Satuan</div>
                <div class="detail-value">Rp @Model.HargaSatuan.ToString("N0")</div>
            </div>
            <form asp-action="Delete" method="post">
                <input type="hidden" asp-for="Id" />
                <div class="button-group">
                    <a asp-action="Index" class="btn-custom btn-secondary-custom">← Batal</a>
                    <button type="submit" class="btn-custom btn-danger-custom">🗑️ Hapus</button>
                </div>
            </form>
        </div>
    </div>
</div>


